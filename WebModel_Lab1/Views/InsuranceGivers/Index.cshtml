@model IEnumerable<WebModel_Lab1.Models.InsuranceGiver>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.BankCountry)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.InsuranceAmount)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IsBank)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.InsuranceObjectNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.BankCountry)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.InsuranceAmount)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsBank)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.InsuranceObjectNavigation.Usreou)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.InsuranceUsreou">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.InsuranceUsreou">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.InsuranceUsreou">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
